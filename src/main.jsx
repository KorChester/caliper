import React from 'react';
import ReactDOM from 'react-dom/client';
import CaliperApp from './CaliperApp.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CaliperApp />
  </React.StrictMode>
);
